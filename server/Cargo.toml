[package]
name = "gutoe-server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
tower = "0.4"
tower-http = { version = "0.5", features = ["fs"] }
tokio = { version = "1", features = ["full"] }
